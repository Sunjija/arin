# 한사코치

한국사능력검정시험 심화 학습 서비스. 목표 방향은 **시험일까지 개념 이해 → 확인 문제 → 누적 복습 → 실전 적용**을 이어주는 학습 코치다.

## 에이전트·Cursor 작업 시작

[AGENTS.md](AGENTS.md) → [전체 개발 로드맵·우선순위](docs/development-roadmap.md) → [최신 작업 지시](docs/parallel-redesign/README.md) → [정책·공통 계약](docs/parallel-redesign/contracts.md) → [소유권](docs/parallel-redesign/ownership.md) → 담당 brief 순으로 읽는다.
Cursor 자동 진입 규칙은 `.cursor/rules/hansacoach.mdc`. 이전 기반 고정 브랜치와 아카이브 지시를 새 작업에 사용하지 않는다.

## 현재 구현과 다음 작업

2026-09-11, `codex/arin-design-v3`의 `34c0f10` 감사 기준. 최신 main/미병합 PR/배포 상태와 구분한다.

- 단원 18 · 자체 문항 100 · 카드 95 · 연표 사건 70. 실전 대비 충분성을 검증한 규모는 아니다.
- 자료실에 선사·고조선 설명과 해당 단원 확인 문제 6개가 있다. 확인 문제 결과는 아직 화면 상태에만 저장된다.
- 현재 오늘 학습은 카드 → 개념 → 문제. 진도는 달력 기반이고 오늘 복습은 같은 시대로 제한된다. **이것은 차기 수정 대상**이다.
- 학습 기록은 브라우저 IndexedDB에 저장된다. 별도 PR의 동기화 구현을 이 브랜치의 완료 기능으로 간주하지 않는다.
- Pretendard와 기존 디자인을 유지한다. 서비스명은 한사코치이며 기존 arin 저장소/주소/ID를 임의 변경하지 않는다.

다음 순서: 기존 PR 검수·콘텐츠 공백 조사 → 공통 학습 계약·기록 이전 → 선사·고조선 한 단원에서 목표·개념·문제·누적 복습·기록 통합 검증 → 전 범위 콘텐츠·실전 검증. 동기화·모바일·결제는 후순위로 재검수한다.
[개발 로드맵](docs/development-roadmap.md)에 전체 작업 ID, 우선순위, 담당 역할, 선행 조건, 완료 기준과 기존 PR 처리 방향을 정리했다. 작업 목록은 배정 완료를 뜻하지 않는다.

GitHub 품질 검사와 정기 점검은 설치되어 있다. 실행 방법과 한계는 [자동화 운영](docs/automation.md)을 참고한다.

## 실행·검증

Node 환경과 저장소의 패키지 관리 흐름을 확인한 뒤 의존성을 설치한다. 현재 저장소에는 npm/pnpm 잠금파일이 함께 있으므로 이번 문서 개정에서 삭제하거나 갱신하지 않는다. CI/팀의 선택을 확인하고 한 도구로 일관되게 작업한다.

```bash
npm run dev
npm test
npm run typecheck
npm run lint
npm run build
```

개발 주소: http://127.0.0.1:43127. 기존 의존성이 없고 npm 흐름을 사용할 때는 `npm ci`를 사용한다.
문서만 수정할 때는 링크·참조·현재 상태/목표 구분을 검증한다. 코드 변경 시 관련 회귀 검증과 위 검사를 수행하고 미실행 검사를 명시한다.

## 화면

| 경로 | 역할 |
|---|---|
| `/` | 오늘 학습과 기록 요약 |
| `/study` | 진행 중 학습과 재개 |
| `/cards` | 카드·복습·오답 |
| `/library` | 개념·연표 (`/timeline` 호환) |
| `/mock` | 10문항 연습·50문항 실전 구조 |
| `/progress` | 학습 기록 |
| `/settings` | 설정·백업·복원 |

## 품질과 데이터 보존

- 현재 `difficulty`는 배점 역할을 한다. 실측 난이도와 분리할 예정이다. 50문항 ID 패딩 금지, 중복 제출 방지·시험 스냅샷 보존.
- 자동 검사 결과는 역사적 사실·실전 변별력의 검수 완료가 아니다. [검수 지침](agents/question-inspector.md)을 따른다.
- 사실·이미지 권리를 확인하고 기출 문장을 그대로 복제하지 않는다. 합격 확률·안정권을 근거 없이 주장하지 않는다.
- DB 삭제/샘플 복원으로 사용자 기록을 덮어쓰지 않는다. 명시적 마이그레이션·백업 호환성을 검증한다.
- `artifacts/`는 생성 리포트/과거 초안, `docs/archive/`는 폐기된 계약이다. 현재 작업 명령과 구분한다.

`.openai/hosting.json`의 기존 Site를 재사용한다. GitHub 푸시와 공개 배포는 별개이며 배포 성공 확인 없이 공개 반영을 보고하지 않는다.
