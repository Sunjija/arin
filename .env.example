# 브라우저·앱이 빌드 시점에 넣는 공개 값만 둔다. 비밀키·서명 비밀번호는 여기에 적지 않는다.

# demo | live
VITE_RUNTIME_MODE=demo

# B 개발 서버가 생기면 설정. 비우면 로컬 IndexedDB만 사용.
VITE_API_BASE_URL=

# 앱 라이브 리로드용 Vite 주소. capacitor `server.url`은 CAPACITOR_LIVE_RELOAD_URL로 주입.
VITE_DEV_SERVER_URL=http://127.0.0.1:43127
