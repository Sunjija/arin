# 배포 접근과 버전 확인

2026-09-11 P0-05 및 P1-03 후속 점검. **이번 복습 수정은 GitHub 푸시 대상이며 공개 배포를 완료하지 않았다.**

- 기존 project ID: `appgprj_6aa12019f9cc819182bfcf0fc375a52b`
- 기존 주소: https://arin-history-study.jguyerscv9.chatgpt.site/
- 설정: `.openai/hosting.json`, 정적 산출물 `dist`
- 현재 연결에서 `get_site` → `NOT_FOUND / project_not_found (404)`, `list_sites(limit: 50)` → 빈 목록.
- P1-03 완료 시 새로 제공된 Sites 연결로 같은 project ID를 다시 조회했으나 `get_site`가 동일한 404를 반환했다. 위 빈 목록은 앞선 P0 조회 결과이며 이번에는 ID를 재사용해 직접 조회했다.
- 이 결과만으로 사이트 삭제나 공개 URL 중단을 단정하지 않는다. 현재 연결 계정/워크스페이스에서 기존 프로젝트를 찾지 못한 상태다.
- 접근 제한을 우회하거나 대체 프로젝트를 만들지 않았다. 기존 hosting.json과 주소를 유지한다.

필요한 조치: 기존 Site를 만든 계정·워크스페이스로 Sites 연결을 확인/복구한다. 연결 복구 전에는 배포 완료라고 표시하지 않는다. 과거 마지막 성공 기록은 `dbf609f` / 배포 v2이나 이번에 native 배포 조회로 재확인하지 못했으므로 복구 대상 후보로만 취급한다.

로컬 검증: P1-03 통합 소스의 239개 테스트·타입·lint·Vite 프로덕션 빌드 통과. Sites 실행 프로필은 portable/configured=false로 기존 구성을 보존했다. Sites 빌드 도우미는 기존 `package-lock.json`과 `pnpm-lock.yaml`이 함께 있어 중단되었다. 잠금파일을 임의 삭제하지 않고 현재 CI가 사용하는 TypeScript/Vite 빌드를 직접 실행했다. 배포 접근 복구 후 패키지 관리자 정리는 별도 검토한다.

## 접근 복구 후 배포 순서

1. 기존 project ID를 조회하고 실제 현재 배포 ID·공개 설정·연결 계정을 확인한다.
2. 배포할 커밋에서 타입·lint·회귀·빌드·콘텐츠 감사와 P1-06 수용 검사를 실행한다.
3. 직전 정상 배포의 커밋·빌드/콘텐츠 버전·DB 버전을 기록하고 GitHub 푸시와 배포를 별도 상태로 관리한다.
4. 기존 프로젝트에 새 버전을 배포하고 배포 ID·URL·빌드 SHA를 기록한다. 공개 범위는 기존 설정을 확인한다.
5. 공개 URL에서 홈 → 첫 단원 설명 → 문제 → 재개 → 자료실 → 기록을 확인하고 로컬 화면과 혼동하지 않는다.
6. 문제가 생기면 호스팅이 지원하는 이전 정상 버전 복구 경로를 먼저 확인한다. DB v4를 만든 브라우저에 구형 DB 앱을 무작정 덮어쓰면 열리지 않을 수 있으므로 **DB v4와 호환되는 복구 빌드**를 사용한다. 기록 삭제로 해결하지 않는다.

실제 배포·되돌리기 명령은 접근 복구 후 해당 호스팅의 지원 API를 확인해서 실행한다. 지금 존재하지 않는 배포 ID나 동작이 검증되지 않은 rollback 명령을 적지 않는다.
