# 한사코치 — 에이전트 공통 작업 지침

기준일: 2026-09-11. 사람의 현재 요청을 우선한다. 이 파일은 개발 방향이며 미구현 기능을 구현 완료로 간주하지 않는다.

## 읽기 순서

1. `docs/development-roadmap.md`: 전체 작업 ID·우선순위·선행 조건·완료 기준
2. `docs/parallel-redesign/README.md`: 현재 상태·작업 순서·기존 PR
3. `docs/parallel-redesign/contracts.md`: 학습 정책과 공통 데이터 설계 게이트
4. `docs/parallel-redesign/ownership.md`: 소유 경로와 제출 규칙
5. 배정받은 `docs/parallel-redesign/briefs/` 지시문
콘텐츠 작업은 `agents/question-inspector.md`도 읽는다. `docs/archive/`와 `artifacts/`의 과거 리뷰는 현행 명령이 아니다.

## 제품 원칙

- 시험일까지 개념 이해 → 확인 문제 → 누적 복습 → 실전 적용을 연결한다.
- 새 개념은 설명부터 시작한다. 이미 배운 범위만 시대를 넘어 복습한다.
- 진도는 학습 완료 기준. 결석으로 미완료 개념을 건너뛰지 않는다.
- 하루 분량은 개념·문제·복습 개수로 표시한다. 실측 없는 시간을 약속하거나 일반 학습 속도로 숙련도를 일괄 감점하지 않는다.
- 시험일, 개념 완주일, 최종 복습 기간을 구분한다. 공식 일정은 출처·확인일과 함께 관리한다.
- 배점과 난이도·변별력을 구분한다. 자동 검사 통과나 익숙한 문항 정답률로 실전 대비 완료·합격 확률을 주장하지 않는다.
- 현재 Pretendard와 기존 디자인을 유지한다. 과거 초록색 디자인 사양으로 되돌리지 않는다.
- 실제 유물 식별용 이미지에는 이용 조건을 확인한 실물 자료를 우선한다. 생성 PNG는 브랜드·설명용, 재현은 표시하고 검수한다.

## 변경·통합 원칙

- 새 일을 시작할 때 `git status`, 브랜치, 원격 HEAD와 관련 열린 PR을 확인한다. main·현재 작업 브랜치·미병합 PR·공개 배포를 구분한다.
- 로드맵의 작업 ID와 의존성을 확인한다. 우선순위·권장 담당 표는 작업 배정이 아니다. 완료 시 커밋/PR·수용 기준 증거와 함께 상태를 갱신한다.
- 기존 PR의 계약·구현을 읽고 재사용 여부를 기록한다. 중복 서비스/별도 기록 저장소를 만들지 않는다.
- 공유 타입·DB·세션 API는 총괄이 먼저 계약과 마이그레이션을 구현·검증한다. 미확정 필드를 담당자가 각각 정의하지 않는다.
- 역할마다 별도 브랜치/작업 디렉터리를 사용한다. 같은 체크아웃을 동시 수정하지 않는다. 에이전트 자동 실행은 이 문서만으로 요청된 것이 아니다.
- 사용자 기록 삭제·reseed로 마이그레이션을 대체하지 않는다. 중복 제출 방지, 스냅샷, 재개, 백업 호환성을 보존한다.
- 배정 범위 외 변경은 의존성으로 보고한다. 임의 병합·강제 푸시·배포·계정/결제 확대를 하지 않는다.
- 사용자 변경과 생성 리포트를 무분별하게 커밋하지 않는다. 잠금파일을 정리 목적으로 변경하지 않는다.

## 검증과 보고

- 관련 회귀 테스트, 타입 검사, lint, 빌드. 코드 변경 시 정확한 실행 결과와 남은 실패를 보고한다. 문서만 바꾸면 링크·참조·정책 충돌을 검증한다.
- 요청된 화면 QA는 모바일·데스크톱·키보드·긴 글·빈 상태·저장 실패를 포함한다. 실행하지 않은 검증을 통과로 쓰지 않는다.
- 제출: 기준 SHA / 변경 파일 / 수용 기준별 증거 / 계약·마이그레이션 영향 / 미해결 사항 / 커밋·PR / 배포 여부.
